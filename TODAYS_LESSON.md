# 📚 풀스택 웹 서비스 배포 및 수익화 실무 총정리

오늘 하루 만에 로컬에서 돌던 코드를 실제 서비스로 탈바꿈시키며 겪은 모든 핵심 기술과 개념을 정리했습니다. 나중에 다른 프로젝트를 할 때 이 문서가 훌륭한 나침반이 될 것입니다.

---

## 1. 프론트엔드 심화 (JavaScript & UI)
* **Local Storage (로컬 스토리지)**: 사용자가 브라우저를 껐다 켜도 데이터가 날아가지 않도록 컴퓨터에 데이터를 저장하는 기법입니다. (북마크, 최근 히스토리 구현에 사용됨) `JSON.stringify`로 글자 화 시켜 넣고, 꺼낼 땐 `JSON.parse`로 다시 객체로 조립하는 것이 핵심 패턴입니다.
* **비동기 타이머 (룰렛 애니메이션)**: `setInterval`로 0.1초마다 텍스트를 마구 바꾸다가, `clearInterval`로 타이머를 부수며 멈추게 하는 역동적인 UI 연출 기법입니다.
* **사용자 데이터 분석 (GA4 Tracking)**: 페이지 맨 위에 구글 애널리틱스 스크립트를 달아놓고 버튼을 누를 때마다 `gtag('event', ...)` 코드를 실행해, 사람들이 한식, 중식 중 뭘 제일 좋아하는지 통계를 내는 "데이터 기반 기획"의 기초를 다졌습니다.

## 2. 서버 인프라 및 컨테이너 (Docker & GCP)
* **Docker (도커)**: "내 컴퓨터에선 되는데 서버에선 안 되는데요?" 라는 변명을 원천 차단하는 기술입니다. 내 폴더 전체를 웹서버(Nginx)와 함께 통조림(Image)처럼 꽝꽝 얼려서, 도커 허브를 거쳐 서버로 배달했습니다.
* **클라우드 서버 (GCP Compute Engine)**: 24시간 내내 켜져 있는 튼튼한 컴퓨터를 빌리고, 주소가 바뀌지 않도록 **고정 IP**를 발급받아 연결했습니다.
* **방화벽과 포트 (Port)**: 해커를 막기 위해 철문을 닫아두되, 웹 서비스가 반드시 오가야 하는 **80포트(HTTP)**와 **443포트(HTTPS)**만 구멍을 뚫어 트래픽을 허용하는 보안 기초를 배웠습니다.

## 3. 꿈의 자동 배포 (CI/CD - GitHub Actions)
* **지속적 통합 및 배포 (CI/CD)**: 수동으로 FTP로 파일을 옮기고 서버 껐다 켜는 노가다를 없애는 기술. 내가 깃허브에 코드를 `push` 완료하는 순간, 깃허브 로봇이 `.github/workflows/deploy.yml` 대본을 읽고 **자동으로 이미지를 굽고(Build) -> 서버에 원격 접속해서(SSH) -> 새 버전으로 갈아끼우는 방식**입니다.
* **보안 환경 변수 (Secrets)**: 소스코드에 비밀번호(IP, 도커허브 비번, RSA 키)를 하드코딩하면 큰일 납니다. 이를 깃허브 설정창 안전 금고(Secrets)에 넣어두고 `${{ secrets.xxx }}` 형태로 치환해서 쓰는 실무 비밀번호 관리법을 적용했습니다.
* **불사조 컨테이너**: 서버 재부팅 시에도 도커가 알아서 컨테이너를 부활시키도록 `--restart unless-stopped` 파라미터를 사용했습니다.

## 4. 네트워크 & 수익화 마케팅 (DNS & AdSense & SEO)
* **도메인 연결 (DNS A레코드)**: 네이버, 구글처럼 사람들이 외우기 쉬운 이름표 주소(`whattoeat.shop`)를 사고, DNS 서버에 "이 이름은 아까 그 고정 IP 아이피를 향합니다"라고 길 안내 표지판(A 레코드)을 붙였습니다.
* **웹 보안 (Cloudflare HTTPS)**: 지금 세팅하고 계신 클라우드 플레어는 중간에서 악성 봇 공격을 막아주며 브라우저 창에 **안전한 연결(자물쇠 🔒)** 모양인 HTTPS 인증서를 무료로 무조건 씌워주는 아주 강력하고 실무적인 CDN 방어막입니다!
* **애드센스 통과를 위한 SEO 전략 (검색엔진 최적화)**: 텍스트가 부족한 서비스는 구글이 광고상(Publisher)으로 취급하지 않아 심사에서 반려됩니다. 이를 방지하기 위해 심리학 및 영양학적 근거를 담은 **'푸드 칼럼 매거진' 텍스트를 HTML 하단에 대량 심어두어** 구글 봇(Crawler)을 기분 좋게 통과시키는 합법적 우회(?) 전략을 배웠습니다.
